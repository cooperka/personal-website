language: node_js

node_js:
  - '12'

cache: yarn

script:
  - yarn run lint
  - yarn test

deploy:
  provider: script
  skip_cleanup: true
  script: yarn run deploy --token "$FIREBASE_TOKEN" -m 'Deployed by Travis CI.'
